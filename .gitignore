# Extracted Grim Dawn game files (~5 GB) - regenerate with `just extract`
/extracted/

# Optional long-format dump (~32 MB) - regenerate with `just parse --duckdb`
/data/devotion_records.csv

# Raw game-data deposit (parquet). Never committed: lives in GitHub Releases
# pinned by deposit.lock - `just fetch-deposit` anywhere, or regenerate with
# `just deposit` on Windows. See docs/deposit.md.
/data/deposit/

# Derived typed item schema (parquet). Same home: GitHub Releases, never git -
# `just fetch-deposit`, or regenerate anywhere with `just derive`.
/data/derived/

# Python / uv
__pycache__/
*.pyc
.venv/
.uv/

# OS
.DS_Store
Thumbs.db
.superpowers/

# Local editor / agent settings (machine-specific, not shared)
.claude/*
# ...except shared workflow skills, which are project content, not machine settings
!.claude/skills/

# Scratch space for temporary/LLM-generated files (mockups, notes) - not committed
.llm/

# Web app
web/node_modules/
web/dist/
web/bun.lockb
web/bun.lock

# Cloudflare Worker local dev: `wrangler dev --local`'s state directory, and the per-developer
# ALLOWED_ORIGIN override for testing against `just serve` (see worker/README.md). Nothing in
# .dev.vars is secret today, but it is wrangler's convention for local-only var files and never
# committed, so that stays true if it ever does hold one.
worker/.wrangler/
worker/.dev.vars

# Precomputed cover table (build artifact, regenerated from data/devotions.json)
/data/cover-table.bin

# Reachability WASM core (build artifact, regenerated from web/wasm with `just wasm`)
/data/reach.wasm

# Harvested false-dim cases (regenerated by `just harvest-false-dims`, not a committed fixture)
/web/test/fixtures/false-dims.json

# Optimized devotion art (committed for the GitHub Pages build; regenerate with `just assets`)
data/deposit
data/derived
